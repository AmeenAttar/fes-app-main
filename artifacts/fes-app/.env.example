EXPO_PUBLIC_DOMAIN=http://localhost:8080

# Same logical site tier as api-server FESCENTER_BASE_URL (hostname for UI labels only —
# investigator detail links still come from APIs). Bump with backend on cutover off /test.
EXPO_PUBLIC_FESCENTER_BASE_URL=https://fescenter.org/test
